<?xml version="1.0" encoding="utf-8"?>
<!-- androdr-089 fixture: the SMS-stealing banker OTP-theft combo (Mamont pattern).
     Declares RECEIVE_SMS (real-time interception of incoming OTP texts) AND binds a
     NotificationListenerService (reading OTP/2FA notifications). Sideloaded via adb,
     so is_sideloaded = true. Should trigger "SMS + Notification Access (Sideloaded)". -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.RECEIVE_SMS" />
    <application android:label="Message Sync">
        <service
            android:name="com.androdr.fixture.smsnotif.Svc"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE"
            android:exported="false">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>
    </application>
</manifest>
